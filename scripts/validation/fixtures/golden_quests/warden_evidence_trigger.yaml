id: warden_evidence_trigger
fixture_mode: static_contract_example
summary: "warden は例外的な scope/security evidence trigger だけを評価する"
input:
  scope_drift: true
  high_risk_triggers:
    - security
  failed_checks: []
expected:
  worker_id: warden
  decision_authority: false
  terminal_worker: true
  next_action: invoke_security_review
  triggers_present:
    - scope_drift
    - security
  triggers_absent:
    - numeric_confidence
    - repeated_failure
  output_contract:
    evidence_state_only: true
  forbidden:
    implementation: true
    acceptance_decision: true
    fabricated_trigger: true
